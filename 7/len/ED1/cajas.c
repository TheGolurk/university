#include<stdio.h>

int main(int argc, char const *argv[])
{
    int a, b, c;
    scanf("%d %d", &a, &b);

    int c[a];

    for (size_t i = 0; i < a; i++)
    {
        scanf("%d", &c[i]);
    }

    for (size_t i = 0; i < a; i++)
    {
        
    }
    
    

    return 0;
}
